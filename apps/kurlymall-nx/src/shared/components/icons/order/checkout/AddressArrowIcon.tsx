import styled from '@emotion/styled';

import { CheckoutAddressArrowImg } from '../../../../images';

export default styled.span`
  display: inline-block;
  width: 8px;
  height: 14px;
  margin: 4px 0 0 3px;
  background: url('${CheckoutAddressArrowImg}') 0 0 no-repeat;
  vertical-align: top;
`;
