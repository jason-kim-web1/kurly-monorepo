import styled from '@emotion/styled';

import { LinkArrowRight } from '../../images';

export default styled.span`
  display: inline-block;
  width: 16px;
  height: 16px;
  background-image: url(${LinkArrowRight});
  background-size: 'cover';
  background-position: 'center';
`;
