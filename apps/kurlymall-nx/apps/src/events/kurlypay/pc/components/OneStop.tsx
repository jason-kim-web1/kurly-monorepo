import EventImage from '../../shared/components/EventImage';
import { ONE_STOP_TEXT } from '../../shared/constants/alternativeText';

export default function OneStop() {
  return <EventImage imageName="img_oneStop" imageHeight={'1889px'} altText={ONE_STOP_TEXT} isPC />;
}
