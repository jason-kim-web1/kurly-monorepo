import styled from '@emotion/styled';

import { CloseIconImg } from '../../images';

export default styled.span`
  width: 44px;
  height: 44px;
  display: inline-block;
  background-image: url(${CloseIconImg});
  background-size: cover;
  background-position: center;
`;
