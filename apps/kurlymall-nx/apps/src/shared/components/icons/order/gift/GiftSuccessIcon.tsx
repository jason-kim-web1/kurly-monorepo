import styled from '@emotion/styled';

import { iconGiftSuccess } from '../../../../images';

export default styled.span`
  display: block;
  width: 60px;
  height: 60px;
  margin: 0 auto;
  background: url('${iconGiftSuccess}') 0 0 no-repeat;
`;
