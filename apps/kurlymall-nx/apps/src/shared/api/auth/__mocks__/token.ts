export const fetchToken = jest.fn().mockResolvedValue({
  accessToken: 'abcde',
});

export const fetchSession = jest.fn().mockResolvedValue({
  accessToken: 'abcde',
});

export const putSession = jest.fn().mockResolvedValue({
  accessToken: 'abcde',
});
